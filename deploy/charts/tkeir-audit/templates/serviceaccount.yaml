{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tkeir-audit.serviceAccountName" . }}
  labels:
    {{- include "tkeir-audit.labels" . | nindent 4 }}
{{- end }}
