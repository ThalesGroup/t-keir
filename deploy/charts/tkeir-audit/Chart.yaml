apiVersion: v2
name: tkeir-audit
description: T-KEIR audit API + WORM archiver (tkeir-audit)
type: application
version: 0.1.0
appVersion: "2.0.0"
dependencies:
  - name: tkeir-lib
    version: "0.1.0"
    repository: "file://../tkeir-lib"
