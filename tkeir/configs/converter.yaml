logger:
  logging-level: info
converter:
  settings:
    output:
      zip: true
    ocr:
      enabled: true
      mode: tesseract
      min-image-pixels: 10000
      min-page-text-chars: 40
      render-dpi: 200
