apiVersion: v1
kind: Service
metadata:
  name: {{ include "tkeir-audit.fullname" . }}
  labels:
    {{- include "tkeir-audit.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      name: http
  selector:
    {{- include "tkeir-audit.selectorLabels" . | nindent 4 }}
