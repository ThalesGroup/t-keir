# Dependency packages that may carry copyleft-family licence strings in
# pip-licenses output after legal review. One package name or
# Name==Version (License) per line. Lines starting with # are ignored.
#
# Runtime licence policy for installs remains `make liccheck`
# (tkeir/liccheck.ini). This file only gates the quality-dashboard
# copyleft inventory check.
#
# Entries below are packages already present in the locked tkeir dependency
# set (dev + models groups) as of the quality-dashboard introduction.

certifi
chardet
fqdn
fr-core-news-md
fr-core-news-sm
pymupdf
tqdm
