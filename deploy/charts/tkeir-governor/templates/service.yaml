apiVersion: v1
kind: Service
metadata:
  name: {{ include "tkeir-governor.fullname" . }}
  labels:
    {{- include "tkeir-governor.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tkeir-governor.selectorLabels" . | nindent 4 }}
