{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tkeir-keycloak.serviceAccountName" . }}
  labels:
    {{- include "tkeir-keycloak.labels" . | nindent 4 }}
{{- end }}
