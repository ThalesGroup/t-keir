apiVersion: v1
kind: Service
metadata:
  name: {{ include "tkeir-ingest.fullname" . }}
  labels:
    {{- include "tkeir-ingest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tkeir-ingest.selectorLabels" . | nindent 4 }}
