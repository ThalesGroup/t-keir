apiVersion: v2
name: tkeir-inference
description: Maps PROVIDER=external|ollama|vllm onto UnifiedLLMWrapper env
type: application
version: 0.1.0
appVersion: "2.0.0"
dependencies:
  - name: tkeir-lib
    version: "0.1.0"
    repository: "file://../tkeir-lib"
