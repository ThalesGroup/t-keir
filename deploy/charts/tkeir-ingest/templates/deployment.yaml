apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "tkeir-ingest.fullname" . }}
  labels:
    {{- include "tkeir-ingest.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "tkeir-ingest.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "tkeir-ingest.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "tkeir-ingest.serviceAccountName" . }}
      securityContext:
        {{- include "tkeir-lib.securityContext" . | nindent 8 }}
      containers:
        - name: tkeir-ingest
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- include "tkeir-lib.containerSecurityContext" . | nindent 12 }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          env:
            - name: INGEST_HOST
              value: "0.0.0.0"
            - name: INGEST_PORT
              value: {{ .Values.service.port | quote }}
            {{- range $k, $v := .Values.env }}
            {{- if $v }}
            - name: {{ $k }}
              value: {{ $v | quote }}
            {{- end }}
            {{- end }}
          {{- if .Values.existingSecret }}
          envFrom:
            - secretRef:
                name: {{ .Values.existingSecret }}
          {{- end }}
          livenessProbe:
            {{- include "tkeir-lib.livenessProbe" (dict "port" "http" "path" "/health" "initialDelaySeconds" 60) | nindent 12 }}
          readinessProbe:
            {{- include "tkeir-lib.readinessProbe" (dict "port" "http" "path" "/ready" "initialDelaySeconds" 30) | nindent 12 }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: ingest-data
              mountPath: /var/tkeir/ingest
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: ingest-data
          {{- if .Values.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "tkeir-ingest.fullname" . }}
          {{- else }}
          emptyDir: {}
          {{- end }}
        - name: tmp
          emptyDir: {}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
