apiVersion: v1
kind: Service
metadata:
  name: {{ include "tkeir-hmi.fullname" . }}
  labels:
    {{- include "tkeir-hmi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tkeir-hmi.selectorLabels" . | nindent 4 }}
