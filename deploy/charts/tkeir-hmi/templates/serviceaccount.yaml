{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tkeir-hmi.serviceAccountName" . }}
  labels:
    {{- include "tkeir-hmi.labels" . | nindent 4 }}
{{- end }}
