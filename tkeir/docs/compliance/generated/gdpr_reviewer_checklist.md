<!-- Generated by compliance/opa/scripts/gen_doc_results.py — do not edit by hand. -->


# GDPR — reviewer checklist

**Version context:** from last audit (passed 10 · violations 33 · not mandatory 0).

> **Not legal advice.** `[x]` means OPA currently passes the check (automatic evidence and/or an explicit attestation). `[ ]` means a human must still close the gap. Items marked **Legal / reviewer (not code)** cannot be fixed by shipping code alone.

## Process

**How a reviewer / legal counsel checks an unchecked item**

1. Confirm the obligation is actually met for your deployment (policy, contract,
   RoPA, DPIA, ENISA process, CE/DoC, …) — **do not invent compliance**.
2. Set the matching field under `compliance/opa/overrides.yaml` to `true`
   (for example `gdpr.attestation.dpia: true` or
   `cra.attestation.encryption_at_rest_and_transit: true`).
3. Leave the field `null` until assessed; use `false` only when you have
   assessed that the control is absent.
4. Re-run `make audit-compliance` (or `make ci`). The checkbox becomes
   `[x]` when OPA sees the attestation (or engineering evidence).

**How engineering closes an unchecked automatic item**

1. Follow the remediation (Makefile target, missing file, or config flag).
2. Re-run `make audit-compliance` so `input.evidence.*` flips to `true`.
3. No `overrides.yaml` change is required for pure evidence checks.

## Already satisfied (10)

- [x] **`Art.5(1)(c)`** — Data minimisation (request_hash only).  
  _Fix by:_ **Engineering / automatic**  
  _How to check:_ Already satisfied by automatic repository evidence (`evidence.action_schema_present`). No action required unless the control regresses.

- [x] **`Art.5(1)(e)`** — Storage limitation (retention configured).  
  _Fix by:_ **Engineering / automatic**  
  _How to check:_ Already satisfied by automatic repository evidence (`evidence.audit_worm_retention_set`). No action required unless the control regresses.

- [x] **`Art.5(1)(f)`** — Integrity and confidentiality.  
  _Fix by:_ **Engineering or legal attestation**  
  _How to check:_ Already satisfied by automatic repository evidence (`evidence.privacy_py_present`). No action required unless the control regresses.

- [x] **`Art.17`** — Right to erasure — crypto-shredding.  
  _Fix by:_ **Engineering / automatic**  
  _How to check:_ Already satisfied by automatic repository evidence (`evidence.privacy_py_present`). No action required unless the control regresses.

- [x] **`Art.22`** — Automated decision-making safeguards / human approval.  
  _Fix by:_ **Engineering or legal attestation**  
  _How to check:_ Already satisfied by automatic repository evidence (`evidence.governor_approvals_present`). No action required unless the control regresses.

- [x] **`Art.25`** — Data protection by design and by default.  
  _Fix by:_ **Engineering or legal attestation**  
  _How to check:_ Already satisfied by automatic repository evidence (`evidence.privacy_py_present`). No action required unless the control regresses.

- [x] **`Art.32(1)(a)`** — Pseudonymisation and encryption.  
  _Fix by:_ **Engineering / automatic**  
  _How to check:_ Already satisfied by automatic repository evidence (`evidence.privacy_py_present`). No action required unless the control regresses.

- [x] **`Art.32(1)(b)`** — Ongoing confidentiality, integrity, availability.  
  _Fix by:_ **Engineering / automatic**  
  _How to check:_ Already satisfied by automatic repository evidence (`evidence.audit_worm_retention_set`). No action required unless the control regresses.

- [x] **`Art.32(1)(c)`** — Ability to restore data after incident.  
  _Fix by:_ **Engineering or legal attestation**  
  _How to check:_ Already satisfied by automatic repository evidence (`evidence.rollback_target_in_makefile`). No action required unless the control regresses.

- [x] **`Art.32(1)(d)`** — Process for regular testing.  
  _Fix by:_ **Engineering or legal attestation**  
  _How to check:_ Already satisfied by automatic repository evidence (`evidence.audit_verify_target`). No action required unless the control regresses.

## Open — legal / reviewer only (31)

These articles use `source: attestation` in the OPA catalogue. Closing them is a **human / legal** decision recorded in `overrides.yaml` → `gdpr.attestation.*`.

- [ ] **`Art.5(1)(a)`** — Lawfulness, fairness, transparency.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest lawfulness_fairness_transparency. Then set `gdpr.attestation.lawfulness_fairness_transparency` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.5(1)(b)`** — Purpose limitation.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest purpose_limitation. Then set `gdpr.attestation.purpose_limitation` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.5(1)(d)`** — Accuracy.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest accuracy. Then set `gdpr.attestation.accuracy` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.6`** — Legal basis for processing documented.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest legal_basis_documented. Then set `gdpr.attestation.legal_basis_documented` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.9(1)`** — Special categories not processed without Art.9(2) basis.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest special_categories_basis. Then set `gdpr.attestation.special_categories_basis` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.12`** — Transparent communication to data subjects.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest transparent_communication. Then set `gdpr.attestation.transparent_communication` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.13`** — Information provided at collection.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest information_at_collection. Then set `gdpr.attestation.information_at_collection` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.14`** — Information where data not collected from subject.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest information_not_from_subject. Then set `gdpr.attestation.information_not_from_subject` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.15`** — Right of access implemented.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest right_of_access. Then set `gdpr.attestation.right_of_access` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.16`** — Right to rectification.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest right_to_rectification. Then set `gdpr.attestation.right_to_rectification` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.18`** — Right to restriction of processing.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest right_to_restriction. Then set `gdpr.attestation.right_to_restriction` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.19`** — Notification obligation on erasure/restriction.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest notification_on_erasure. Then set `gdpr.attestation.notification_on_erasure` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.20`** — Right to data portability.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest right_to_portability. Then set `gdpr.attestation.right_to_portability` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.21`** — Right to object.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest right_to_object. Then set `gdpr.attestation.right_to_object` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.26`** — Joint controller agreement.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest joint_controller_agreement. Then set `gdpr.attestation.joint_controller_agreement` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.27`** — Representative in EU designated.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest representative_in_eu. Then set `gdpr.attestation.representative_in_eu` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.28`** — Processor agreement in place.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest processor_agreement. Then set `gdpr.attestation.processor_agreement` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.29`** — Processing under controller/processor authority only.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest processing_under_authority_only. Then set `gdpr.attestation.processing_under_authority_only` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.30`** — Records of processing activities (RoPA).  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest records_of_processing. Then set `gdpr.attestation.records_of_processing` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.31`** — Cooperation with supervisory authority.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest cooperation_supervisory_authority. Then set `gdpr.attestation.cooperation_supervisory_authority` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.33`** — Data breach notification to SA ≤72h.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest breach_notification_72h. Then set `gdpr.attestation.breach_notification_72h` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.34`** — Communication to data subjects.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest communicate_to_subjects. Then set `gdpr.attestation.communicate_to_subjects` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.35`** — Data protection impact assessment (DPIA).  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest dpia. Then set `gdpr.attestation.dpia` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.36`** — Prior consultation with supervisory authority.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest prior_consultation. Then set `gdpr.attestation.prior_consultation` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.37`** — DPO designated where required.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest dpo_designated. Then set `gdpr.attestation.dpo_designated` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.38`** — DPO position and resources.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest dpo_position_resources. Then set `gdpr.attestation.dpo_position_resources` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.39`** — DPO tasks.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest dpo_tasks. Then set `gdpr.attestation.dpo_tasks` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.44`** — Transfers only with adequate protection.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest transfers_adequate_protection. Then set `gdpr.attestation.transfers_adequate_protection` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.45`** — Adequacy decision basis.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest adequacy_decision_basis. Then set `gdpr.attestation.adequacy_decision_basis` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.46`** — Appropriate safeguards (SCCs, BCR).  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest appropriate_safeguards. Then set `gdpr.attestation.appropriate_safeguards` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

- [ ] **`Art.49`** — Derogations for specific situations.  
  _Fix by:_ **Legal / reviewer (not code)**  
  _How to check:_ Attest transfer_derogations. Then set `gdpr.attestation.transfer_derogations` in `overrides.yaml` to `true` after legal/privacy sign-off. See process below.

## Open — engineering / automatic (1)

- [ ] **`Art.5(2)`** — Accountability demonstrated.  
  _Fix by:_ **Engineering / automatic**  
  _How to check:_ Run `make audit-evidence`. Re-run the audit; no legal attestation field is required.

## Open — hybrid (code **or** attestation) (1)

- [ ] **`Art.24`** — Controller responsibility and accountability.  
  _Fix by:_ **Engineering or legal attestation**  
  _How to check:_ Prefer engineering: Run `make audit-evidence` / maintain gdpr.md. Alternatively, after reviewer confirmation, set `gdpr.attestation.compliance_gdpr_doc` in `overrides.yaml` to `true`.
