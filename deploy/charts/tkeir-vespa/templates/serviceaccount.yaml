{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tkeir-vespa.serviceAccountName" . }}
  labels:
    {{- include "tkeir-vespa.labels" . | nindent 4 }}
{{- end }}
