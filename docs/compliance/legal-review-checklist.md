# GDPR & CRA legal / reviewer checklist

> **Not legal advice.** Snapshot generated by
> `compliance/opa/scripts/gen_doc_results.py` after each
> `make audit-compliance` / `make ci`.

- **`[x]`** — already satisfied by automatic repository evidence and/or a prior
  attestation.
- **`[ ]` + Legal / reviewer (not code)** — cannot be closed by shipping code;
  a human must confirm the obligation and set the matching field in
  `compliance/opa/overrides.yaml`.
- **`[ ]` + Engineering** — fix with Makefile / artefacts, then re-audit.

--8<-- "./docs/compliance/generated/legal_review_checklist.md"
