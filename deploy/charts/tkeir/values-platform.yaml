# Profile: platform (P4) — extends secure

global:
  env: prod

vespa:
  resourceProfile: production

governor:
  enabled: true
  mode: enforce

audit:
  enabled: true

keycloak:
  enabled: true

# Kubeflow wired by installer detection (Phase 7/8)
kubeflow:
  enabled: true
