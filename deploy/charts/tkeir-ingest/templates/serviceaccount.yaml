{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "tkeir-ingest.serviceAccountName" . }}
  labels:
    {{- include "tkeir-ingest.labels" . | nindent 4 }}
{{- end }}
